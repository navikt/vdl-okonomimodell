models:
  - name: stg__tertial
    columns:
      - name: ar_tertial
        tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error
          - dbt_utils.at_least_one:
              config:
                severity: error
