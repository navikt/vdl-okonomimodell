models:
  - name: scd2_oebs__segment
    columns:
      - name: pk_scd2_oebs__segment
        tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error
      - name: ek_scd2_oebs__segment
        tests:
          - unique:
              where: gyldig_til > current_timestamp
              config:
                severity: error
