models:
  - name: scd2_oebs__hierarki
    columns:
      - name: pk_scd2_oebs__hierarki
        tests:
          - unique
          - not_null
      - name: ek_scd2_oebs__hierarki
        tests:
          - unique:
              where: gyldig_til > current_timestamp
              config:
                severity: error
